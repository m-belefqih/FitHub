<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Required to read images on Android 13+ (API 33) -->
    <!-- Request this at runtime on SDK >= 33 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!--
        Read external storage for Android <= 12 (API <= 32)
        Keep android:maxSdkVersion="32" so this permission is ignored on API 33+
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" /> <!-- Legacy write permission (only meaningful on API <= 28) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitHub">
        <activity
            android:name=".activity.UpdateProfileActivity"
            android:exported="false" />
        <activity
            android:name=".activity.WorkoutSessionActivity"
            android:exported="false" />
        <activity
            android:name=".activity.TrainingActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ExerciseListActivity"
            android:exported="false" />
        <activity
            android:name=".activity.WorkoutListActivity"
            android:exported="false" />
        <activity
            android:name=".activity.MuscleListActivity"
            android:exported="false" />
        <activity
            android:name=".activity.CreatePostActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activity.EmailVerificationActivity"
            android:exported="false" />
        <activity
            android:name=".activity.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:exported="false" />
    </application>

</manifest>